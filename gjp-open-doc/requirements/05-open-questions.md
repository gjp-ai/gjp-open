# Requirements Open Questions

- Should Audio and Files be visible in the main tab bar for the first release?
- Should the app support favorites locally without login?
- Should the API support `search` and `tag` aliases to match the existing web wrapper, or should all clients move to canonical query names?
- What app store region, age rating, category, support email, and privacy policy should be used?
