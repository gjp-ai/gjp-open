# Implementation Decisions

- Confirm mobile repository name.
- Confirm native Android + native iOS as the implementation direction, or explicitly switch to the Expo shared-codebase alternative.
- Confirm app name, app icon source, bundle identifier, and Android package name.
- Confirm primary navigation: whether Audio and Files are tabs or More items.
- Confirm API filter strategy: canonical params only, or API aliases for `search`/`tag`.
- Confirm store accounts, privacy policy URL, and support email.
